jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "sithu"
          password: "Sithu@123"
        - id: "jenkinsUser"
          password: "user123"

  authorizationStrategy:
    globalMatrix:
      permissions:
        # Admin user (full administrator)
        - "Overall/Administer:sithu"

        # Non-admin user (restricted)
        - "Overall/Read:jenkinsUser"
        - "Job/Read:jenkinsUser"
        - "Job/Build:jenkinsUser"
        - "Job/Workspace:jenkinsUser"
        - "View/Read:jenkinsUser"
